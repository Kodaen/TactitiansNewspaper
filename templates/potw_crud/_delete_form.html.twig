<form method="post" action="{{ path('p_o_t_w_crud_delete', {'id': p_o_t_w.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ p_o_t_w.id) }}">
    <button class="btn">Delete</button>
</form>
